# Persistent repository instructions

Read `COWORK_TASK.md` first and treat it as the acceptance contract.

Work autonomously. Repair rather than ask about ordinary engineering choices. Preserve scientific integrity and provenance labels. Never fabricate live or hardware evidence. Never run hardware unless `NOISEVAULT_ALLOW_HARDWARE=1` already exists. Never commit credentials.

Use current public APIs. Record material methodology changes in `reports/AUTONOMOUS_RUN_LOG.md`. Keep generated report values synchronized with machine-readable results. Finish with `reports/FINAL_HANDOFF.md` and a clean git worktree.
